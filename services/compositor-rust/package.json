{
  "name": "@repo/compositor-rust",
  "private": true,
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg --out-name compositor --scope repo",
    "test": "bash ./scripts/test.sh"
  }
}
