export const database = null;
export const hasDatabaseConnection = false;

export enum ModuleCategory {
  GALLEY = "GALLEY",
  ISLAND = "ISLAND",
  L_SHAPE = "L_SHAPE"
}

export type Prisma = Record<string, unknown>;
